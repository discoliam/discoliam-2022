---
title: 'Contact | Discoliam'
description: 'Get in touch about your project.'
layout: 'layouts/page.html'
eleventyNavigation:
  key: Contact
  order: 4
---

<div class="Container">

  <div class="Page-Title">
    <h1>{{contact.page_title | safe}}</h1>
    <p class="Lead">{{contact.intro| safe}}</p>
  </div>

  <div class="Contact Grid">
    <div class="Contact__Form">
      <form id="ContactForm" name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" data-netlify-recaptcha="true">
        <div class="sr-only">
          <label>
            Don’t fill this out if you’re human: <input name="bot-field" />
          </label>
        </div>

        <div class="input-wrap">
          <label for="name">Your Name: <abbr title="required">*</abbr></label>
          <input type="text" name="name" placeholder="Firstname Surname" required />
        </div>

        <div class="input-wrap">
          <label for="email">Your Email: <abbr title="required">*</abbr></label>
          <input type="email" name="email" required placeholder="hello@example.com" />
        </div>

        <div class="input-wrap">
          <label for="message">Message: <abbr title="required">*</abbr></label>
          <textarea name="message" placeholder="..." required></textarea>
        </div>

        <div data-netlify-recaptcha="true"></div>

        <input type="hidden" name="subject" value="Enquiry from Otaku Website" />

        <button type="submit" class="Button">Send</button>

      </form>
      <div class="Contact__Message">
        <p class="Contact__MessageText Contact__MessageText--success">{{contact.success}}</p>
        <p class="Contact__MessageText Contact__MessageText--error">{{contact.error}}</p>
      </div>
    </div>
    <div class="Contact__Info">
      <h2 class="h4">Location</h2>
      <p translate="no" typeof="schema:PostalAddress" class="postal-address">
        <span class="sr-only" property="schema:name">{{site.name}}</span>
        <span property="schema:streetAddress">{{site.streetAddress | safe}}</span>
        <span property="schema:addressLocality">{{site.addressLocality}}</span>
        <span property="schema:addressRegion">{{site.addressRegion}}</span>
        <span property="schema:postalCode">{{site.postalCode}}</span>
        <span class="sr-only" property="schema:addressCountry">United Kingdom</span>
      </p>
      <h2 class="h4">Email</h2>
      <p class="Contact__Email">
        <a href="mailto:{{site.email}}" target="_blank" rel="noopener">{{site.email}}<span class="sr-only">- This is an email link</span></a>
      </p>
      <h2 class="h4">Phone</h2>
      <p class="Contact__Phone">
        <a class="tel" href="tel:{{site.phone}}" target="_blank" rel="noopener">{{site.phone_stylised}}</a>
      </p>
    </div>
  </div>



</div>
